services:
  backend:
    build: .
    ports:
      - "5001:5001"
    volumes:
      - ./TestDatabase:/app/TestDatabase
    environment:
      - ASPNETCORE_ENVIRONMENT=Development
    restart: unless-stopped
